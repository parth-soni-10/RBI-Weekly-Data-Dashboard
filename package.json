{
  "name": "rbi-weekly-dashboard-netlify",
  "version": "1.2.0",
  "description": "Netlify functions for RBI weekly reserves scraper + live crude oil data + macro context (FII/FPI flows, FX intervention, G-Sec, REER, tax receipts)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check-syntax.js",
    "fetch:data": "node scripts/fetch-all.js"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "node-xlsx":  "^0.23.0"
  }
}
