[build]
  publish = "."
  functions = "netlify/functions"

[[functions]]
  function = "fetch-crude"
  node_bundler = "esbuild"
  timeout = 20

[[functions]]
  function = "fetch-data"
  node_bundler = "esbuild"
  timeout = 26